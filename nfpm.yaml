name: kepubify
arch: amd64
platform: linux
version: ${VERSION}
section: utils
priority: optional

replaces:
provides:
depends:
recommends:
suggests:
conflicts:

maintainer: Patrick Gaskin <patrick@pgaskin.net>
description: |
  Kepubify converts EPUBs to KEPUBs.
vendor: Patrick Gaskin
homepage: https://pgaskin.net/kepubify
license: MIT

bindir: /usr/bin
files:
  ./build/kepubify-linux-64bit: /usr/bin/kepubify
  ./build/covergen-linux-64bit: /usr/bin/covergen
  ./build/seriesmeta-linux-64bit: /usr/bin/seriesmeta
config_files:
